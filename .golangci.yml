linters-settings:
  errcheck:
    exclude-functions:
      - fmt.Fprintf
      - fmt.Fprintln
      - fmt.Fprint
      - (io.Writer).Write
      - os.Setenv
      - os.Unsetenv
      - os.Remove
      - os.WriteFile
      - (*os.File).Close
      - (encoding/json.Encoder).Encode
